## 📌 Features
- CNN architecture with multiple convolutional and dense layers
- Dropout layers to prevent overfitting
- L2 regularization on convolutional and dense layers
- Trained on Fashion-MNIST dataset (28x28 grayscale images)
- Easy to use for prediction or further training

Install required packages:
pip install tensorflow numpy matplotlib

📊 Results
Achieves around 90–92% test accuracy (can vary based on training)
Generalizes well due to dropout and L2 regularization

⚡ Future Improvements
Data augmentation for better generalization
Experiment with deeper architectures or ResNet-like models
Hyperparameter tuning for improved accuracy
